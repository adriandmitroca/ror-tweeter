- provide(:title, @title)
.row
  %aside.col-md-4
    %section.stats
      = render 'shared/user_stats'
      - if @users.any?
        .user_avatars
          - @users.each do |user|
  .col-md-8
    %h3= @title
    - if @users.any?
      = render @users
